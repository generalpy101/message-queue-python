syntax = "proto3";

package apinode;

service ApiNode {
  // For broker or control-plane usage (optional)
  rpc GetStatus(StatusRequest) returns (StatusResponse);
}

message StatusRequest {}

message StatusResponse {
  string node_id = 1;
  int32 connected_clients = 2;
  repeated string topics = 3;
}
